#!/bin/bash

########
# INFO #
########


##########
# CONFIG #
##########


#############
# FUNCTIONS #
#############

set_wslconf() {
  if [[ -f /etc/wsl.conf ]]; then
    echo "==> wsl encontrado"
    return
  fi
  if [[ ! -f /etc/wsl.conf ]]; then
    echo "==> Copiando configuración y saliendo"
    wget https://raw.githubusercontent.com/carcheky/ccktools/master/config/wsl.conf
    sudo mv wsl.conf /etc/
  fi
  exit 1
}

set_dirs() {
  if [[ -d /c/wsl/ ]]; then
    echo "==> Existe la ruta /c/wsl"
    return
  fi
  if [[ ! -d /c/wsl/ ]]; then
    sudo mkdir '/c/wsl/'
    sudo mkdir '/c/wsl/user'
    sudo mkdir '/c/wsl/sites'
    sudo chown -R user:user '/c/wsl/'
    cd '/c/wsl/user'
    git clone https://github.com/carcheky/ccktools.git
  fi
}


################
# MAIN PROGRAM #
################

  
set_wslconf
set_dirs



