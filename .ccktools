#!/bin/bash

cd /c

wsl_update(){
if [[ -d /c/wsl/user/ccktools ]]; then
  dir = $(pwd)
  git pull
  cd ${dir}
fi
}

wsl_update

source ~/ccktools/.ccktools_aliases
source ~/ccktools/.ccktools_functions

mountdesadrupal